# 技术栈

> 本文档完整列出项目使用的技术栈：运行环境、依赖版本、外部服务与模型。
> 版本来源：`pyproject.toml`（最低约束）与 `uv.lock`（实际锁定）/ `frontend/package.json`。
> 开发流程、配置与排障见《开发文档.md》，组件机制图解见《组件工作机制.md》。

---

## 1. 总览

| 层级 | 技术 | 版本（锁定） | 项目内职责 |
|---|---|---|---|
| 语言运行时 | Python / Node.js | 3.12 / ≥18 | 后端 / 前端 |
| 包管理 | uv / npm | — | 后端依赖与虚拟环境 / 前端依赖 |
| Web 框架 | FastAPI + Uvicorn + Pydantic | 0.139.2 / 0.51.0 / 2.13.4 | REST API + SSE 流式 |
| LLM 接入 | OpenAI SDK / zai-sdk | 2.48.0 / 0.2.3 | DeepSeek 与本地兼容端点 / 智谱文本与视觉 |
| 大模型服务 | DeepSeek · 智谱 GLM · vLLM · Ollama | — | 文本生成（可切换 provider）；深度思考模型 |
| 视觉模型 | 智谱 glm-4.6v-flashx | — | 图片内容解析 |
| 嵌入模型 | BAAI/bge-small-zh-v1.5（sentence-transformers 5.6.1 / torch 2.13.0 / transformers 5.14.1） | — | Dense 语义编码（512 维） |
| 精排模型 | BAAI/bge-reranker-base（CrossEncoder） | — | 检索结果重排序 |
| 分词 | jieba | 0.42.1 | BM25 稀疏编码分词 |
| 向量库 | Qdrant（qdrant-client） | 服务端 1.18 / 客户端 1.18.0 | Named Vectors 混合检索（Prefetch + RRF） |
| 图数据库 | Neo4j（neo4j 驱动） | 驱动 6.2.0 / 服务端 5.3+ 或 6.x | 知识图谱 6 类查询 |
| 关系数据库 | PostgreSQL + SQLAlchemy + psycopg | SQLAlchemy 2.0.51 / psycopg 3.3.4 | 招投标数据查询 + 会话/反馈存储 |
| 联网搜索 | Tavily（tavily-python） | 0.7.27 | 关键词搜索（basic 深度，进程内缓存） |
| 语义搜索 | Exa MCP Server（npx 拉取） | — | 经自研 MCP 客户端调用 |
| HTTP 客户端 | httpx | 0.28.1 | OpenAI SDK/自研出站客户端底层传输 |
| 前端框架 | Next.js（App Router） | ^14.2.0 | SSR 框架 + 开发代理 |
| UI 库 | React + TypeScript + Tailwind CSS | ^18.3.0 / ^5.5.0 / ^3.4.0 | 界面 |
| Markdown 渲染 | react-markdown + remark-gfm | ^9.0.0 / ^4.0.1 | 回答渲染（含 GFM 表格） |
| 图标 | lucide-react | ^0.400.0 | UI 图标 |
| 数据处理 | pandas + openpyxl + ipykernel | 3.0.5 / 3.1.5 / 7.3.0 | Excel 管道与 notebook |
| 配置 | python-dotenv | 1.2.2 | .env 加载（override=False） |
| 测试 | pytest | 9.1.1 | 131 例测试 |
| CI | GitHub Actions | — | push/PR 自动跑测试 |
| 文档 | Markdown + Mermaid | — | 机制图解（flowchart 语法） |

---

## 2. 运行时与依赖管理

| 项 | 选择 | 说明 |
|---|---|---|
| Python | **3.12**（`requires-python = ">=3.12"`，`.python-version` 固定 3.12） | 后端全部代码 |
| 依赖管理 | **uv**（含 `uv run` / `uv sync`） | 锁文件 `uv.lock`；dev 组单独管理 pytest |
| PyPI 源 | 清华镜像 `https://pypi.tuna.tsinghua.edu.cn/simple` | pyproject 内声明为默认源（国内网络） |
| Node.js | **≥ 18** | 前端构建与运行 |
| npm | 标准 npm（`package-lock.json`） | 前端依赖 |

---

## 3. 后端框架层

### 3.1 FastAPI + Uvicorn

- **FastAPI 0.139.2**：全部 11 个端点定义于 `api/server.py`；Pydantic 2.13.4 负责请求/响应模型与枚举校验（如 feedback 的 `rating: Literal["up","down"]`）
- **Uvicorn 0.51.0**（`[standard]` 附加项含 websockets/httptools）：生产 ASGI 服务器；`main.py api` 开发模式带 `reload=True`
- **端点模型约定**：全部端点用同步 `def`——FastAPI 自动将同步端点放入线程池执行，慢请求（LLM 生成数十秒）不阻塞事件循环，限流中间件与健康检查始终可用
- **流式**：`StreamingResponse`（`text/event-stream`），响应头 `Cache-Control: no-cache` + `X-Accel-Buffering: no`（防反向代理缓冲）

### 3.2 HTTP 传输

- **httpx 0.28.1**：OpenAI SDK 与 zai-sdk 的底层传输；项目自建 `src/http_client.py` 统一出站策略——`trust_env=False` 不继承系统代理，显式配置 `HTTP_PROXY`/`HTTPS_PROXY` 才走代理
- **requests**（Tavily 客户端用）：同策略封装（`build_requests_session`）

---

## 4. LLM 层

### 4.1 接入 SDK

| SDK | 版本 | 服务 |
|---|---|---|
| **openai** | 2.48.0 | DeepSeek（OpenAI 兼容协议）+ vLLM / Ollama（OpenAI 兼容端点） |
| **zai-sdk** | 0.2.3 | 智谱 GLM 文本与视觉 |

统一接口 `src/clients/base_client.py`：`chat` / `chat_stream` / `chat_stream_thinking`（思考与正文分离流式）/ `chat_raw`（Agent 读取 tool_calls 用）；工厂 `llm_factory.get_llm_client(provider)` 路由。

### 4.2 模型清单

| Provider | 模型 | 用途 | 关键参数 |
|---|---|---|---|
| DeepSeek | `deepseek-v4-flash`（默认） | 文本主模型（Function Calling） | temperature 0.3 |
| DeepSeek | `DEEPSEEK_THINKING_MODEL`（可选，如 `deepseek-v4-pro`） | 深度思考推理模型 | max_tokens 8192；**省略 tool_choice**（该参数返回 400） |
| 智谱 | `glm-4.7-flashx`（默认） | 文本备选 provider | 同 DeepSeek 接口语义 |
| 智谱 | `ZHIPU_THINKING_MODEL`（可选） | 深度思考备选 | 同上 |
| vLLM | `VLLM_MODEL`（默认 `deepseek-r1-0528-qwen3-8b`） | 本地推理（OpenAI 兼容） | 超时 300s（本地 8B 推理慢） |
| Ollama | `OLLAMA_MODEL`（默认 `deepseek-r1:8b`） | 本地推理 | **跳过 Function Calling**，直接走 RAG 降级路径 |
| 智谱 Vision | `glm-4.6v-flashx` | 图片内容解析（`/api/vision`） | base64 data URL 输入 |

- 前端右上角可逐请求切换 provider（deepseek / zhipu / vllm / ollama），偏好存 localStorage
- 推理模型的 `reasoning_content` 处理：流式时思考与正文分离（thinking/token 两类事件）；正文为空时思考内容兜底为回答

---

## 5. 嵌入与重排模型（Hugging Face）

| 模型 | 角色 | 细节 |
|---|---|---|
| `BAAI/bge-small-zh-v1.5` | Dense 查询/文档编码 | 512 维；查询带指令前缀「为这个句子生成表示以用于检索相关文章：」；L2 归一化；GPU OOM 自动降级 CPU |
| `BAAI/bge-reranker-base` | CrossEncoder 精排 | 对（query, answer）对打分；召回 30 条后取 top-k |
| jieba BM25 | Sparse 编码 | 精确模式分词（过滤单字）；BM25 权重 → `{indices, values}` 稀疏向量 |

- 载体：**sentence-transformers 5.6.1** + **transformers 5.14.1** + **torch 2.13.0**（CUDA 可用时走 GPU）
- 模型懒加载 + 双重检查锁（并发首用只加载一份）；首次运行从 HF 下载（国内 `HF_ENDPOINT=https://hf-mirror.com`）

---

## 6. 数据存储

### 6.1 Qdrant（向量库）

- 服务端 **1.18**（Cloud 或自建），客户端 `qdrant-client 1.18.0`
- **Named Vectors**：每点同时存 `dense`（512 维，COSINE）+ `sparse`（BM25）两路向量
- 混合检索经 `query_points` 的 `Prefetch`（双路各召回 30）+ `RrfQuery`（RRF k 按问题类型 30/60/90）原生融合
- 版本注意：`client.search()` 在 qdrant-client ≥1.9 已移除，全部走 `query_points`
- 客户端参数：`trust_env=False`（不继承系统代理）、30s 超时、断连重试 2 次

### 6.2 Neo4j（图数据库）

- 驱动 **neo4j 6.2.0**；服务端 **5.3+**（需支持 `begin_transaction(timeout=...)` 服务端超时）或 6.x
- 星型 schema：SubjectMatter 中心 + Purchaser/Supplier/Agency/Location + HAS_TRANSACTION
- 连接池：max_connection_pool_size=50，max_connection_lifetime=3600

### 6.3 PostgreSQL（关系数据库）

- 驱动：**SQLAlchemy 2.0.51**（Core 用法，仅 `text()` 参数化查询）+ **psycopg 3.3.4**（binary 包）
- 业务表 `bidding_procurement`（18 字段白名单）；`conversations`（messages JSONB）与 `feedback` 表由客户端连接时自动创建
- 安全：字段/聚合函数白名单、参数化 SQL、LIKE 通配符转义（`ESCAPE '\'`）

---

## 7. 联网搜索与 MCP

| 组件 | 技术 | 说明 |
|---|---|---|
| 关键词搜索 | **Tavily Search API**（tavily-python 0.7.27） | `search_depth=basic`（1 积分/次）；`include_answer=True` 取 AI 摘要；进程内缓存 TTL 600s / 上限 512 条；仅成功结果缓存 |
| 语义搜索 | **Exa MCP Server**（`npx exa-mcp-server`） | 基于嵌入的网页搜索，适合深度内容；后台线程启动，未就绪时自动降级为仅 Tavily |
| MCP 协议栈 | **自研轻量客户端**（`src/mcp/mcp_client.py`） | stdio JSON-RPC 2.0，protocolVersion `2024-11-05`；请求-响应串行化加锁；stderr 后台排空防管道死锁；60s 单响应超时；进程崩溃自动重启（30s 限频） |

---

## 8. 前端技术栈

| 类别 | 依赖 | 版本 | 说明 |
|---|---|---|---|
| 框架 | next（App Router） | ^14.2.0 | `rewrites` 把 `/api/*` 代理到后端 :8001；SSE 流式**绕过代理直连**（代理会缓冲响应） |
| UI | react / react-dom | ^18.3.0 | `React.memo` + ref 稳定回调优化 token 级重渲染 |
| 语言 | typescript | ^5.5.0 | 严格类型 + `PersistedMessage` 运行时校验（脏记录丢弃） |
| 样式 | tailwindcss + postcss + autoprefixer | ^3.4.0 / ^8.4.0 / ^10.4.0 | 暗色模式 `class` 策略，`<head>` 内联脚本防闪烁 |
| Markdown | react-markdown + remark-gfm | ^9.0.0 / ^4.0.1 | 回答渲染（GFM 表格），链接强制 `target=_blank` + `noopener` |
| 图标 | lucide-react | ^0.400.0 | 全部 UI 图标 |
| 类型 | @types/node / @types/react / @types/react-dom | ^20.14.0 / ^18.3.0 / ^18.3.0 | TS 类型支持 |

---

## 9. 数据处理与评测工具

| 组件 | 版本 | 用途 |
|---|---|---|
| pandas | 3.0.5 | Excel 读写与清洗（batch 管道、ingest、to_postgresql） |
| openpyxl | 3.1.5 | Excel 引擎 |
| ipykernel | 7.3.0 | `batch/批处理标的物.ipynb` 交互式 notebook |
| pytest | 9.1.1 | 131 例测试（同步风格，无 asyncio 插件） |
| python-dotenv | 1.2.2 | `.env` 加载（`override=False`：真实环境变量优先） |

评测线（eval/）：`ragas_eval.py`（LLM-as-Judge：Faithfulness/Relevancy/Recall）、`agent_eval.py`（工具选择准确率 + 延迟）、`dashboard.py`（feedback 与检索缓存统计）。

---

## 10. 外部服务依赖汇总

| 服务 | 必需性 | 配置键 | 说明 |
|---|---|---|---|
| Qdrant（Cloud/自建 1.18） | **必需** | `QDRANT_URL` / `QDRANT_API_KEY` | 启动校验，缺失即退出 |
| DeepSeek API | 必需（默认 provider） | `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` / `DEEPSEEK_MODEL` | OpenAI 兼容协议 |
| 智谱开放平台 | 可选 | `ZHIPU_API_KEY` / `ZHIPU_MODEL` | 文本 + 视觉共用 key |
| Tavily | 可选 | `TAVILY_API_KEY` | 联网开关开启后挂载 |
| Exa MCP | 可选 | `EXA_API_KEY` | npx 拉取 exa-mcp-server |
| Neo4j（5.3+ / 6.x） | 可选 | `NEO4J_*` | 未连接时图谱工具降级 |
| PostgreSQL | 可选 | `POSTGRES_*` | 未连接时数据库工具与会话存储降级 |
| vLLM / Ollama | 可选 | `VLLM_*` / `OLLAMA_*` | 本地推理 |
| Hugging Face | 间接必需 | `HF_TOKEN` / `HF_ENDPOINT` | 模型下载（镜像） |

---

## 11. CI 与文档工具

| 项 | 选择 | 说明 |
|---|---|---|
| CI | GitHub Actions（ubuntu-latest） | `actions/checkout@v4` + `astral-sh/setup-uv@v5`；push/PR 到 main 执行 `uv sync` + `pytest` |
| 机制图解 | Mermaid `flowchart`（v10/v11 稳定语法） | 《组件工作机制.md》20 个图解：direction / subgraph / classDef / style，18 种子图配色 |
| 文档 | Markdown（GFM） | README / CLAUDE / 开发文档 / 技术栈 / 组件工作机制 |

---

## 12. 版本兼容性注记（踩坑记录）

| 约束 | 原因 |
|---|---|
| qdrant-client ≥1.9 | `client.search()` 已移除，必须 `query_points` + `Prefetch`/`RrfQuery` |
| Neo4j 服务端 ≥5.3 | `begin_transaction(timeout=...)` 服务端终止慢查询依赖此版本 |
| 深度思考模型省略 `tool_choice` | DeepSeek 推理模型对该参数返回 400，省略即 auto 语义 |
| `load_dotenv(override=False)` | 容器/CI 注入的环境变量不被本地 `.env` 顶掉 |
| 出站 `trust_env=False` | 桌面代理软件关闭后，默认信任环境代理会把全部出站连接路由到失效端口（WinError 10061） |
| `CrossEncoder.predict()` 默认已套 Sigmoid | 单标签模型（num_labels=1）的 `activation_fn` 默认为 `Sigmoid`，**其返回值已是 0-1 概率**（实测 logit 5.38 → 0.9954）。再对它做一次归一化即成双重 sigmoid：logit −5.9 的不相关候选会被抬到 **0.50**，分数彻底失去区分度。需要原始 logit 时显式传 `activation_fn=nn.Identity()`（见 `src/rag/embedder.py` 的 `Reranker.rerank`） |
| 精排与查询配对的字段 | 用「答案」而非「知识库问题」与用户查询配对，会把 top-1 命中率从 98.7% 打到 78.7%（查询是短问句、答案是大段说明文，文体差距过大）。改动前先跑 `eval/retrieval_eval.py` |
| OpenAI SDK v2 | 项目代码面向 SDK v2 API（`chat.completions.create` 与 `reasoning_content` 属性访问），升级 SDK 大版本需回归客户端测试 |
| pandas 3.x | batch 管道与导入脚本已适配 3.x 清洗行为（NaN 先 dropna 再转字符串） |
